% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_stats.R
\name{spectral_stats}
\alias{spectral_stats}
\title{Calculate Spectral Statistics}
\usage{
spectral_stats(
  wave,
  specimen_id = "",
  total_range = FALSE,
  robust = FALSE,
  db = TRUE,
  cutoff = -20,
  lines = TRUE,
  sound_type = "",
  temp = NULL,
  hpf = 0
)
}
\arguments{
\item{wave}{A \code{Wave} object representing the sound data.}

\item{specimen_id}{Character string for the specimen identifier.}

\item{total_range}{Logical, whether to calculate the full frequency range.}

\item{robust}{Logical, whether to use a robust frequency resolution (244 Hz).}

\item{db}{Logical. If TRUE, a decibel scale is used. If FALSE (default),
a linear scale is used. Both are relative to the peak amplitude in the Wave.}

\item{cutoff}{Numeric, threshold for bandwidth calculation. If db = FALSE,
set between 0 and 1. If db = TRUE, set to a negative dB value.}

\item{lines}{Logical, whether to add lines for min, max, and peak
frequencies.}

\item{sound_type}{Character string for the type of sound analyzed.}

\item{temp}{Numeric, optional, temperature in degrees Celsius.}

\item{hpf}{Numeric, optional, high-pass filter cutoff frequency in kHz.
Defaults to 0.}
}
\value{
A list containing:
\itemize{
\item \code{data}: A tibble with calculated statistics.
\item \code{plot}: An interactive \code{plotly} plot with frequency and amplitude traces.
}
}
\description{
This function calculates various spectral statistics for a complete Wave
object, including peak frequency, bandwidth, spectral entropy, spectral
flatness, spectral excursion, and spectral energy. It generates an
interactive plot with frequency and amplitude traces and includes options
to visualize key statistics with lines on the plot.
}
\examples{
\dontrun{
wave <- readWave("example.wav")
result <- spectral_stats(wave)
result$plot
}

}
